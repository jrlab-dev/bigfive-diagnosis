# ビッグファイブ診断サイト 引き継ぎ
最終更新：2026-06-08（セッション188）

> 新しいセッションではこのファイルを最初に読む。
> 技術仕様は `【引き継ぎ】詳細.md`、過去の作業は `【引き継ぎ】履歴.md` を参照。

---

## ⚠️ R0画像のルール（必ず守ること）

| 項目 | 内容 |
|---|---|
| **対象コード** | 各因子が `{1, 3, 5}` のみ → **3^5 = 243種類** |
| **禁止** | 2や4を含むコード（ガチャで出ない） |
| **根拠** | `gacha-utils.js` の `genCardR0()` → `vals = [1, 3, 5]` |
| **フォルダ** | `_private/ogp_images_v2/M_r0/` と `F_r0/`（各243枚） |
| **スクリプト** | `_private/generate_common_r0.js`（修正済み） |

---

## 次にやること

### 🔧 トップページ デザイン改修（進行中：セッション188）※未完成・未プッシュ

**概要：** Apple風スクロールアニメーションの導入。シンプル化・速度改善。

**実装済み：**
- **ヘッダー**：ロゴ・ホーム削除。「マイページ」「性格診断」「総合レポート」の3リンクのみ。背景・線なし。`nav.js`はindex.htmlではスキップ（GA・テーマ切り替えは動作）
- **ヒーローボタン**：「診断をはじめる」→ `quiz.html?v=10` に直接遷移。ロゴ完成後にフェードイン＋光演出（`btnGlow`キーフレーム）
- **フッター**：© Jr. Genius / ビッグファイブマン。リンク色を黒（#333）に。よくある質問・科学的根拠・ブログ・利用規約・お問い合わせ
- **スクロールアニメーション**（Intersection Observer + CSS @keyframes）：
  - DATA & SCALE：`scale(0.6)` → `scale(1)` で拡大して現れる
  - FEATURES 4枚：クロス方向にスライドイン（左カードは右から、右カードは左から）。1.2秒間隔で順番
  - VOICE 4枚：同上
  - CTA（30問・120問）：同上
- **テーマボタン**：`light-mode.css` の `bottom: 24px` → `64px` に上に移動

**変更ファイル：**
- `index.html`：ヘッダーHTML追加、CSS追加（`.top-header`、`.anim`系、`btnGlow`）、スクロールアニメーションJS追加、CTA修正（10問版削除）、フッター修正
- `nav.js`：index.html判定でヘッダー・フッター生成をスキップ
- `light-mode.css`：`.theme-toggle` の bottom を 64px に

**未解決の課題：**
- Apple風の1つずつ順番に現れる演出は動作確認済みだが、微調整が必要（速度・間隔・発火タイミング）
- ロゴ画像（PNG 6枚・計1.1MB）のWebP化未実施
- ヒーローのCanvas糸アニメーション（約300行JS）を残すか削るか未定
- デプロイ未実施 → 完成後にまとめてpush

**アニメーション設定：**
- animation-duration: 2s
- animation-delay: 0s / 1.2s / 2.4s / 3.6s
- Observer threshold: 0.5
- キーフレーム：`animScale` / `animLeft`(100vw→0) / `animRight`(-100vw→0) / `animUp` / `animShrink`

### ✅ キャラクター画像 一括トリミング（完了：セッション186）
- `images/characters/M/`・`F/`・`hidden/` 全512枚の余白を除去（RGBAアルファ検出）
- **push未実施** → 次回セッション開始時に `git push` すること

### ✅ アニメ本編キャラ フォルダ整備（完了：セッション186）
- 不足8キャラのフォルダ作成＋ちびキャラPNG配置済み
- 社長・御剣の息子のちびキャラはまだ未生成

### ✅ 非R0コード画像フォールバック修正（完了：セッション185）
- `character-registry.js`：30問版/120問版で2や4を含むコードが丸めてR0画像を返すよう修正
- `album.js`：隠しキャラ判定を30問版にも拡張（旧：120問版のみ）
- `card-collection.html`：`hidden_characters.js` 追加 + `AlbumUtils.computeRarityForAlbum` で正しくレアリティ計算

### 📁 アニメ関係リサーチデータ（セッション185）
- `_private/アニメ関係/` にHTML 6ファイル（01〜06）

### 🔧 R5/R4 隠しキャラ 画像生成（Codex）→ 実装

**状況：** ユーザーが生成PNGを手動修正中

- タイプ名・設定：`_private/R5_R4_character_specs.md`（全184キャラ分）
- 画像生成ガイド：`_private/画像生成ガイド.md`
- 生成プロンプト：`_private/画像生成プロンプト_Part01〜05_*.md`
- 保存先：`画像/キャラクター（商用利用可）/combined/`

**手動修正完了後にやること：**
1. 分割スクリプトで F/M に振り分け → `images/characters/` へ配置
2. `result.html` の隠しキャラロジック更新（R5/R4判定）
3. `hidden_characters.js` の更新

### 🔧 リスク選好ゲーム（risk.html）Holt-Laury原法への再設計

**再設計の方針（合意済み）：**
1. 両方ともくじ（A=低リスク、B=高リスク）
2. 確率が10%→100%に変化、金額は固定
3. 設計確定後に risk.html を書き直す

**注意：** risk.html + 統合6ファイル + group.html + pgg.html が未プッシュ。

### 🔧 ガチャ・ゲーミフィケーション系の追加改善
- ログインボーナス仕様を変えたい（具体的な変更内容は未確認）
- PGGの実機確認（コード確認・構文チェックはOK）

### 🔧 X・note.com アカウント開設→SNS運用開始
- ユーザーがアカウントを作ったら声をかけてもらう
- まずnote.comから着手

### 🔧 SEOブログ記事の継続
- **記事総数：125本**（最終：`blog/hitei-bakari-suru-hito.html` 2026-06-05）
- **次の候補：** 悩み系ロングテール（神経症傾向×孤独感など）
