# ビッグファイブ診断サイト 引き継ぎ
最終更新：2026-06-10（セッション193）

> 新しいセッションではこのファイルを最初に読む。
> 技術仕様は `【引き継ぎ】詳細.md`、過去の作業は `【引き継ぎ】履歴.md` を参照。

---

## ⚠️ R0画像のルール（必ず守ること）

| 項目 | 内容 |
|---|---|
| **対象コード** | 各因子が `{1, 3, 5}` のみ → **3^5 = 243種類** |
| **禁止** | 2や4を含むコード（ガチャで出ない） |
| **根拠** | `gacha-utils.js` の `genCardR0()` → `vals = [1, 3, 5]` |
| **フォルダ** | `_private/ogp_images_v2/M_r0/` と `F_r0/`（各243枚） |
| **スクリプト** | `_private/generate_common_r0.js`（修正済み） |

---

## 次にやること

### 🔧 トップページ SEO・速度の残タスク（セッション192の続き）

**完了済み（push済み）：** h1追加・ヒーローにH2「ビッグファイブ診断」・favicon 719KB廃止・CSS preload・テーマFOUC修正（カード初期srcを-light版に）・スマホ糸アニメ軽量化（2倍速/hold=0/実時間ベース）・スクロールstage高さ4割短縮・**ヒーローボタン潰れ修正（flex-shrink:0、セッション193）**・**初訪問者向けRECOMMENDカード（セッション193）**

**残り：**
1. **各セクションのh2補強**（現状h1→h3に飛んでいる。FEATURES/UPGRADE等の見出しをh2に）
2. 他ページのh1有無の点検
3. スクロールstage短縮の操作感フィードバック待ち
4. `index.html.backup-速度改善前` の削除（安定確認後）

**注意：** スマホ速度スコアは60前後が現状の上限（CSS構造の大工事をしない限り）。詳細な原因分析は履歴のセッション192参照。**デザイン変更は事前相談→スクショ確認→pushの順を厳守。**

### ✅ トップページ Apple風スクロールアニメーション（完了：セッション190）

**概要：** Apple製品ページ風のスクロール連動アニメーション。PC版完成・プッシュ済み。

**実装内容：**

1. **スクロール連動アニメーション（5ステージ）**
   - 仕組み：`.scroll-stage`に余剰高さを設定、`.scroll-stage-inner`を`position: sticky`で固定。スクロール量に応じてアニメーション進捗を制御。
   - FEATURES：4枚カードが左右から順次スライドイン（`data-scroll-group`でシーケンシャル）
   - UPGRADE：30問/120問カード。「もっと深く知る」shrink演出→テキストfade-up→カードscale
   - RELATIONSHIP：あの人/チームカード。同じ構成
   - RECOMMEND：コンシェルジュ1件推薦。キャッチコピー付きフルワイドカード
   - VOICE：4枚体験談（RECOMMENDの下に配置）
   - DATA & SCALE：Intersection Observer自動アニメ（scroll-stage外）

2. **アニメーション仕様**
   - `requestAnimationFrame`スロットル
   - `easeOutCubic`イージング
   - Lock-on-reveal（WeakSet）：一度表示された要素は戻らない
   - Stage collapse（scroll-up時のみ）：上にスクロールして画面外に出た完了ステージを折りたたむ
   - Sequential stagger：`start = i / count * 0.8`で順次アニメ

3. **コンシェルジュ改修（concierge.js）**
   - `maxRecommendations: 1`（1件のみ推薦）
   - 解放済み診断（gateなし/gate解除済み）を優先ソート
   - `catchcopy`・`desc`フィールド追加（17診断すべて）
   - dark-triad / riasec を新規登録
   - スクロールステージ内で演出（shrinkタイトル→fade-up説明→scaleカード）

4. **Canvas糸アニメーション修正**
   - `position: fixed`→`absolute`に変更（hero-animatedセクション内に配置）
   - アニメ完了後に`canvas.style.display='none'`で非表示化
   - リサイズ時のリセット防止（`phase !== 'done'`のときのみ`init()`）

5. **ヘッダー**
   - 「マイページ」「ブログ」「性格診断」「総合レポート」の4リンク
   - 白半透明グラデーション背景（スクロール時の文字被り防止）
   - モバイル：中央寄せ＋`flex-wrap`
   - nav.jsスキップ：`window.__SKIP_NAV`フラグ＋パス判定の二重ガード

6. **ロゴ画像パス修正**
   - `.gitignore`で`画像/`が除外されていた→`images/`にコピーして解決
   - `images/logo-part-{E,O,A,C,N}.webp`（各20-40KB）
   - `images/ロゴ.webp`（173KB、高画質版）

**変更ファイル：**
- `index.html`：スクロールアニメーションCSS/JS、CTA・あの人・コンシェルジュのスクロールステージ化、ヘッダー・フッター直接埋め込み、モバイルCSS
- `concierge.js`：1件推薦、解放済み優先、catchcopy/desc追加、dark-triad/riasec追加
- `nav.js`：`__SKIP_NAV`フラグ対応
- `images/`：ロゴパーツ5枚追加

**セクション配置順（上から下）：**
1. ヒーロー（ロゴアニメ＋診断ボタン）
2. DATA & SCALE（統計）
3. FEATURES（4特徴カード）
4. UPGRADE（30問/120問）
5. RELATIONSHIP（あの人/チーム）
6. RECOMMEND（コンシェルジュ）
7. VOICE（体験談）

**未解決の課題：**
- モバイルでスクロールアニメーションの安定性に課題あり（iOS Safariのsticky挙動）
- モバイルのレイアウト微調整がまだ必要かも
- Observer threshold: 0.5
- キーフレーム：`animScale` / `animLeft`(100vw→0) / `animRight`(-100vw→0) / `animUp` / `animShrink`

### ✅ キャラクター画像 一括トリミング（完了：セッション186 / push：セッション189）
- `images/characters/M/`・`F/`・`hidden/` 全512枚の余白を除去（RGBAアルファ検出）
- push完了済み（セッション189）

### ✅ アニメ本編キャラ フォルダ整備（完了：セッション186）
- 不足8キャラのフォルダ作成＋ちびキャラPNG配置済み
- 社長・御剣の息子のちびキャラはまだ未生成

### ✅ 非R0コード画像フォールバック修正（完了：セッション185）
- `character-registry.js`：30問版/120問版で2や4を含むコードが丸めてR0画像を返すよう修正
- `album.js`：隠しキャラ判定を30問版にも拡張（旧：120問版のみ）
- `card-collection.html`：`hidden_characters.js` 追加 + `AlbumUtils.computeRarityForAlbum` で正しくレアリティ計算

### 📁 アニメ関係リサーチデータ（セッション185）
- `_private/アニメ関係/` にHTML 6ファイル（01〜06）

### ✅ R5/R4 隠しキャラ 画像生成（Codex）→ 実装（完了：セッション189）

### ✅ 人間関係相談 reladvice.html（完了：セッション189）
- 保存データから2人を選択（自分＋あの人、あの人同士どちらもOK）
- テーマ選択：好かれたい / 振られた / 距離がある / ケンカが多い
- Big Fiveスコアの静的ロジックで分析・アドバイス生成
- `character-registry.js` + `hidden_characters.js` でキャラクター画像を正しく表示
- `mypage.html`「あの人の結果」タブに導線ボタン追加済み
- `light-mode.css` にカード系ライトモード対応追記済み
- push完了済み

**将来の拡張：** 仕事・友達テーマの追加（現在は恋愛のみ）

### 🔧 リスク選好ゲーム（risk.html）Holt-Laury原法への再設計

**再設計の方針（合意済み）：**
1. 両方ともくじ（A=低リスク、B=高リスク）
2. 確率が10%→100%に変化、金額は固定
3. 設計確定後に risk.html を書き直す

**注意：** risk.html + 統合6ファイル + group.html + pgg.html が未プッシュ。

### 🔧 ガチャ・ゲーミフィケーション系の追加改善
- ログインボーナス仕様を変えたい（具体的な変更内容は未確認）
- PGGの実機確認（コード確認・構文チェックはOK）

### 🔧 X・note.com アカウント開設→SNS運用開始
- ユーザーがアカウントを作ったら声をかけてもらう
- まずnote.comから着手

### 🔧 SEOブログ記事の継続
- **記事総数：125本**（最終：`blog/hitei-bakari-suru-hito.html` 2026-06-05）
- **次の候補：** 悩み系ロングテール（神経症傾向×孤独感など）
